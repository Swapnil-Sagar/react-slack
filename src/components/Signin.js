import React, { Component } from 'react';

export default class Signin extends Component {
  render() {
    return (
      <div className="signin-form">
        <h1>Sign in / Sign-Up</h1>
        <button>Sign in or sign-up with Google</button>
      </div>
    );
  }
}
